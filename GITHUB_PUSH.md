# GitHub Push Instructions

After creating an empty GitHub repository, run these commands from the `CrescendoShield/` folder:

```bash
git remote add origin https://github.com/YOUR_USERNAME/CrescendoShield.git
git branch -M main
git push -u origin main
```

Then deploy on Streamlit Community Cloud:

```text
App entry point: dashboard/app.py
```

